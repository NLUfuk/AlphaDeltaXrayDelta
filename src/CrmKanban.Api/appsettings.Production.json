{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
      }
    }
  },
  "Captcha": {
    "_comment": "Cloudflare Turnstile. SiteKey (public) + SecretKey (secret) live in the LIVE server's appsettings.Production.json only — never in git. Enabled without a secret fails closed and blocks the public form.",
    "Enabled": false,
    "Provider": "turnstile"
  },
  "Email": {
    "Provider": "log",
    "From": "no-reply@crm-kanban.local"
  }
}
